services:
  - type: web
    name: the-var-council
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: OPENCODE_ZEN_API_KEY
        sync: false
